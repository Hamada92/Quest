type: google.api.Service
config_version: 3
http:
  rules:
    - selector: questionspb.QuestionsService.CreateQuestion
      post: /api/questions
      body: "*"

    - selector: questionspb.QuestionsService.GetQuestion
      get: /api/questions/{id}
